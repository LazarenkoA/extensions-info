server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html index.htm;

    # Настройки для React Router
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Проксирование API запросов к backend
    location /api/ {
        # если фронт с бэком в докере то http://backend:8080 (или без докера локально)
        # если фронт в докере, а бэк нет то http://host.docker.internal:8080
        proxy_pass http://host.docker.internal:8080; # http://backend:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }



    # Настройки кэширования для статических файлов
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}